using UnityEngine;
using System.Collections;


/**
 * A class to dynamically add a font to GUI HUD text(only seems to work in built versions)
 */ 
public class FontScript : MonoBehaviour {

	public Font thisFont;
}
